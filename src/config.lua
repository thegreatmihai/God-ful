local config = {
	Enabled = true,
}

local description = {
	Enabled = "Removes the 4-god boon limit, so boons from every god stay available all run. Turn off to restore vanilla behavior. Takes effect immediately, no restart needed.",
}

return config, description
